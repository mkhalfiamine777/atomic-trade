---
description: "Start a new development session by loading project context and status."
trigger: "start session, load context, ابدأ, افتتاح الجلسة"
---

# 🚀 Session Start Protocol (آلية الافتتاح)

> هذا الملف يعمل "كمجس" (Sensor) لبداية الجلسة.

1.  **🔍 Context Check (فحص السياق):**
    -   Run `view_file AI_mkhalfiAmine.md`.
    -   Run `view_file Master_Execution_Plan_2026.md`.
    // turbo
    -   Run `view_file C:\Users\مريم\.gemini\antigravity\brain\0eee0296-0fbf-43ba-ad1c-7b67f579aedc\core_vision_report.md` ← **الوثيقة التأسيسية (الفكرة الأساسية: العرض والطلب على الخريطة)**.

2.  **📋 Task Audit (فحص المهام):**
    -   View active tasks in `active_tasks/`.
    -   Identify the "Immediate Next Step" from `AI_mkhalfiAmine.md`.

3.  **🏗️ Environment Scan (فحص البيئة):**
    -   Check if `npx next dev` is running.
    -   Check essential files specific to the current task.

4.  **📣 Status Report (تقرير الحالة):**
    -   Summarize: "Welcome back! Based on previous sessions, we are at [Current Phase]. Today's objective is [Goal]."
