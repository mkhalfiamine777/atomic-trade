# 📋 تقرير الجلسة 32 — 13 أبريل 2026
**التاريخ:** 13-04-2026 | **التوقيت:** 07:29 → 08:57 (بتوقيت GMT+1)
**المدة:** ~1 ساعة و 30 دقيقة
**الحالة:** ✅ مكتملة | **Build:** ✅ ناجح | **Commit:** `2c754b5`

---

## 🎯 أهداف الجلسة
تعزيز الأمان وصرامة المنصة عبر فرض تحديد الموقع الجغرافي على المستخدمين الجدد، وبناء نظام تجميد/حذف حسابات آمن ومتدرج.

---

## 🚀 الإنجازات المفصلة

### 1. 📍 فرض GPS إجباري عند التسجيل (Enforced Location Tracking)
| الملف | التعديل |
|-------|---------|
| `OnboardingOverlay.tsx` | تعديل زر "لنبدأ" ليطلب صلاحية GPS إجبارياً عبر `navigator.geolocation` قبل إغلاق النافذة |
| `actions/user.ts` | تحديث `completeOnboarding()` لقبول وتخزين `latitude` و `longitude` مباشرة في DB |

**السبب:** المتاجر الجديدة كانت لا تظهر على الخريطة لأن إحداثياتها `null`. الآن لا يمكن لأي مستخدم إكمال التسجيل بدون منح صلاحية الموقع.

### 2. ❄️ نظام تجميد الحساب (Soft Delete — 6 أشهر)
| الملف | التعديل |
|-------|---------|
| `prisma/schema.prisma` | إضافة حقل `scheduledDeletionAt DateTime?` لجدول `User` |
| `actions/user.ts` | إنشاء دالة `scheduleAccountDeletion(phone, password)` مع تحقق مزدوج (bcryptjs) |
| `SettingsDrawer.tsx` | إضافة زر "إيقاف الحساب" مع نافذة تأكيد تطلب رقم الهاتف + الرمز السري |
| `EditProfileModal.tsx` | إضافة "منطقة الخطر" (Danger Zone) بتصميم أحمر تحذيري |

**آلية العمل:**
- المستخدم يدخل رقم هاتفه + رمزه السري ← يتم التحقق ← يُسجَّل تاريخ الحذف (بعد 6 أشهر) ← يُطرد فوراً.
- إذا تراجع وسجّل دخوله خلال المهلة ← يُلغى التجميد تلقائياً.
- إذا مرت 6 أشهر ← يُرفض دخوله نهائياً.

### 3. 🛡️ حارس تسجيل الدخول (Login Gatekeeper)
| الملف | التعديل |
|-------|---------|
| `actions/auth.ts` | إضافة فحص `scheduledDeletionAt` عند تسجيل الدخول مع إلغاء تلقائي أو رفض نهائي |

### 4. 🔧 إصلاحات تقنية
| المشكلة | الحل |
|---------|------|
| `Module not found: bcrypt` | تغيير الاستيراد إلى `bcryptjs` (المتوفرة في المشروع) |
| `EADDRINUSE: port 3000` | إيقاف العمليات المعلقة عبر `Get-NetTCPConnection` |
| `EPERM: prisma engine locked` | إيقاف السيرفر قبل `prisma db push` |

---

## 📊 الملفات المعدلة
| الملف | نوع التعديل |
|-------|------------|
| `web/prisma/schema.prisma` | ✏️ إضافة حقل |
| `web/src/actions/user.ts` | ✏️ دوال جديدة + إصلاح |
| `web/src/actions/auth.ts` | ✏️ فحص أمني |
| `web/src/components/ui/OnboardingOverlay.tsx` | ✏️ فرض GPS |
| `web/src/components/dashboard/SettingsDrawer.tsx` | ✏️ واجهة حذف الحساب |
| `web/src/components/profile/EditProfileModal.tsx` | ✏️ منطقة الخطر |

---

## ⏭️ الخطوات القادمة (الجلسة 33)
1. **اقتصاد الثقة:** بناء عرض "نقاط السمعة" والدرع الذهبي للمتاجر الموثوقة.
2. **التبسيط الوحشي:** مراجعة الصفحات لإزالة أي ميزة لا تخدم التجارة.
3. **CRON Job:** برمجة مهمة دورية للحذف الفعلي للحسابات التي تجاوزت 6 أشهر.
