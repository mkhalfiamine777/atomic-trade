# مهمة تحديث لوحة التحكم إلى خريطة تفاعلية (Dashboard Map Update)
**الحالة:** مكتملة ✅

---

## 🧭 الهدف (Objective)
تحويل صفحة `/dashboard` لتصبح **الواجهة الرئيسية المستندة إلى الخريطة (Map-Based Interface)**، مع الحفاظ على جميع الوظائف السابقة (الرادار، التصنيفات، التنبيهات) وعرضها بشكل بصري تفاعلي.

---

## ✅ قائمة المهام (Checklist)

### المرحلة 1: التأسيس والتنظيف (Foundation)
- [x] **تنظيف صفحة الداشبورد:** إزالة الكود القديم من `src/app/dashboard/page.tsx` (تم عبر `DashboardClient`).
- [x] **إعداد حاوية الخريطة:** ضبط `CSS` للخريطة لتكون `h-full w-full` وإزالة الحواف والحدود من `Map.tsx`.

### المرحلة 2: تكامل الخريطة (Integration)
- [x] **استيراد مكون الخريطة:** تم دمج `Map` داخل `DashboardClient` و `GuestDashboardClient`.
- [x] **تفعيل بيانات المستخدم:** `userId` يمرر بشكل صحيح.
- [x] **التحقق من الرادار:** تم التأكد من منطق `useGeofencing` وهو يعمل بقطر 300م.

### المرحلة 3: تحسين التفاعل (UX Enhancement)
- [x] **إضافة شريط التنقل السفلي:** تم إنشاء `BottomNav.tsx` الجديد ودمجه.
- [x] **ضبط الشاشة:** تم رفع زر `RecenterButton` ليكون `bottom-32` لتجنب التغطية.
- [x] **التحقق من الأيقونات:** تمت مراجعة `market.ts` و `stories.ts` والتأكد من جلب `type` و `reputationScore` للعرض الصحيح على الخريطة.

---

## 📝 ملاحظات الإنجاز (Completion Notes)
- تم تحويل التطبيق بنجاح ليكون **Map-First Application**.
- واجهة الداشبورد الآن تعرض الخريطة بملء الشاشة مع أدوات تحكم عائمة.
- تم ضبط الأيقونات والبث الحي للقصص (Live Stories) لتعمل بتناغم مع الخريطة.
